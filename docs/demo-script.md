# Three-Minute Demo Script

1. Open the dashboard and explain the five approved Security Promise Contracts.
2. Select **Analyze insecure PR**.
3. Point to the Security DNA score changing from 96 to 32 and the merge decision changing to BLOCK.
4. Open the authorization graph and identify the new customer → cross-account access → invoice path.
5. Show the verified HTTP evidence for invoice ownership and manager-only refund approval.
6. Explain that all checks ran against synthetic data in the Shadow Lab.
7. Select **Verify fixed version** and show PASS with zero drift.
8. Open the GitHub Actions workflow and explain how the same decision acts as a pull-request gate.

Closing line:

> AegisTwin X does not trust an AI guess. It uses AI-ready reasoning to discover what should be tested, then requires deterministic evidence before blocking a change.

