# AegisTwin X runs without external AI credentials in deterministic demo mode.
# Add a provider key only when implementing the optional LangGraph assistant.
AEGISTWIN_ENV=development
AEGISTWIN_DATABASE_PATH=data/aegistwin.db

